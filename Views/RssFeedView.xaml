<phone:PhoneApplicationPage x:Class="RssStarterKit.Views.RssFeedView"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            FontFamily="{StaticResource PhoneFontFamilyNormal}"
                            FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}"
                            SupportedOrientations="Portrait"
                            Orientation="Portrait"
                            DataContext="{Binding Path=Main, Source={StaticResource Locator}}"
                            mc:Ignorable="d"
                            d:DesignHeight="768"
                            d:DesignWidth="480"
                            shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot"
          Background="Transparent">

        <controls:Pivot Title="{Binding SelectedFeed.Title}">

            <controls:PivotItem Header="news items">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ListBox x:Name="RssFeedListBox"
                             ItemsSource="{Binding SelectedFeed.Items}" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Last updated: " />
                        <TextBlock Text="{Binding SelectedFeed.RefreshTimeStamp}" />
                    </StackPanel>
                </Grid>
            </controls:PivotItem>

            <controls:PivotItem Header="feed info">
                <Grid>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="{Binding Description}" />
                        <TextBlock Text="{Binding ImageUrl}" />
                        <TextBlock Text="{Binding RefreshTimeStamp}" />
                        <TextBlock Text="{Binding Link}" />
                        <TextBlock Text="{Binding RssUrl}" />
                    </StackPanel>

                </Grid>
            </controls:PivotItem>
        </controls:Pivot>

    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True"
                              IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png"
                                            Text="Refresh"
                                            x:Name="RefreshButton"
                                            Click="RefreshButton_Click" />
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>